package de.projectnash.application.util;

/**
 * This enum provides different request status values in order to give an admin the opportunity to set these values for an incoming {@link Request}.
 * @author Silvio D'Alessandro
 *
 */
public enum RequestStatus {
    WAITING,
    ACCEPTED,
    DENIED
}
